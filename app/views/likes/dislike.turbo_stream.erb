<%= turbo_stream.replace "#{dom_id(@post)}-likes-counter" do  %>
  <%= render "like_counter", post: @post %>
<% end %>

<%= turbo_stream.replace "#{dom_id(@post)}-like-btn" do %>
  <%= render "like_button", post: @post %>
<% end %>
